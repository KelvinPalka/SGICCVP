<Window x:Class="WPF_Projeto_BD.Views.FuncionarioLista"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Funcionários" Height="700" Width="1000"
        Background="#F2F4F7"
        WindowStartupLocation="CenterScreen">

    <Grid>

        <!-- Definições de linha: CABEÇALHO + CONTEÚDO -->
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================= CABEÇALHO ================= -->
        <Border Grid.Row="0" Background="#1F2937">
            <DockPanel VerticalAlignment="Center" Margin="15,0">
                <Button Content="Voltar"
                        Width="120" Height="35"
                        Background="#374151"
                        Foreground="White"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        DockPanel.Dock="Left"
                        Click="BtnVoltar_Click"/>
                <TextBlock Text="Gerenciar Funcionários"
                           FontSize="22"
                           Foreground="White"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="20,0,0,0"/>
            </DockPanel>
        </Border>

        <!-- ================= CONTEÚDO ================= -->
        <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" VerticalAlignment="Top">

                <!-- ================= FUNCIONÁRIOS ================= -->
                <TextBlock Text="Funcionários" FontSize="20" FontWeight="Bold" Foreground="#4A90E2"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="＋ Novo Funcionário"
                            Width="180" Height="38"
                            Background="#3B82F6"
                            Foreground="White"
                            FontWeight="Bold"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="Adicionar_Funcionario"/>
                </StackPanel>
                <Border Background="White" CornerRadius="8" BorderBrush="#D1D5DB" BorderThickness="1" Padding="10">
                    <DataGrid x:Name="dgFuncionarios"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              RowHeight="33"
                              BorderThickness="0"
                              AlternatingRowBackground="#F9FAFB"
                              Background="White">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                            <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                            <DataGridTextColumn Header="Cargo" Binding="{Binding Cargo}" Width="150"/>
                            <DataGridTextColumn Header="Telefone" Binding="{Binding Telefone}" Width="150"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                            <DataGridTextColumn Header="Departamento" Binding="{Binding Departamento}" Width="150"/>
                            <DataGridTemplateColumn Header="Ações" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Editar"
                                                    Width="65" Height="28"
                                                    Background="#3B82F6"
                                                    Foreground="White"
                                                    FontWeight="SemiBold"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Margin="5,0"
                                                    Click="Editar_Funcionario"/>
                                            <Button Content="Excluir"
                                                    Width="65" Height="28"
                                                    Background="#EF4444"
                                                    Foreground="White"
                                                    FontWeight="SemiBold"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Margin="5,0"
                                                    Click="Excluir_Funcionario"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</Window>
