<Window x:Class="WPF_Projeto_BD.Views.ClienteLista"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clientes" Height="600" Width="950"
        Background="#F2F4F7"
        WindowStartupLocation="CenterScreen">

    <Grid>

        <!-- Definições de linha: CABEÇALHO + CONTEÚDO -->
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- Cabeçalho -->
            <RowDefinition Height="*"/>
            <!-- Conteúdo -->
        </Grid.RowDefinitions>


        <!-- ================= CABEÇALHO ================= -->
        <Border Grid.Row="0" Background="#1F2937">
            <DockPanel VerticalAlignment="Center" Margin="15,0">

                <!-- Botão Voltar -->
                <Button Content="Home"
                        Width="120" Height="35"
                        Background="#374151"
                        Foreground="White"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        DockPanel.Dock="Left"
                        Click="BtnVoltar_Click"/>

                <TextBlock Text="Gerenciamento de Clientes"
                           FontSize="22"
                           Foreground="White"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="20,0,0,0"/>
            </DockPanel>
        </Border>


        <!-- ================= CONTEÚDO ================= -->
        <Grid Grid.Row="1" Margin="20" VerticalAlignment="Top">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Botão adicionar -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 10 0 10">
                <Button Content="＋ Novo Cliente"
                        Width="180" Height="38"
                        Background="#3B82F6"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="Adicionar_Cliente"/>
            </StackPanel>


            <!-- Tabela -->
            <Border Grid.Row="1"
                    Background="White"
                    CornerRadius="8"
                    BorderBrush="#D1D5DB"
                    BorderThickness="1"
                    Padding="10" Margin="0,0,0,-426">

                <DataGrid x:Name="dgClientes"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          RowHeight="33"
                          BorderThickness="0"
                          AlternatingRowBackground="#F9FAFB"
                          Background="White">

                    <DataGrid.Columns>

                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                        <DataGridTextColumn Header="Telefone" Binding="{Binding Telefone}" Width="150"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="220"/>

                        <!-- AÇÕES -->
                        <DataGridTemplateColumn Header="Ações" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                                        <Button Content="Editar"
                                                Width="65" Height="28"
                                                Background="#3B82F6"
                                                Foreground="White"
                                                FontWeight="SemiBold"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Margin="5,0"
                                                Click="Editar_Cliente"/>

                                        <Button Content="Excluir"
                                                Width="65" Height="28"
                                                Background="#EF4444"
                                                Foreground="White"
                                                FontWeight="SemiBold"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Margin="5,0"
                                                Click="Excluir_Cliente"/>

                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>

            </Border>

        </Grid>

    </Grid>
</Window>
